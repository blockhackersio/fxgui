{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "CI=1 turbo run test",
    "pub": "pnpm build && changeset && changeset version && changeset publish",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "config": "workspace:*",
    "tsup": "^7.2.0",
    "turbo": "latest",
    "vitest": "^0.34.6"
  },
  "engines": {
    "node": "18"
  },
  "packageManager": "pnpm@8.9.0",
  "name": "fxgui"
}
